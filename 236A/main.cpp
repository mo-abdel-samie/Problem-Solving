#include <bits/stdc++.h>

using namespace std;

int main()
{

    string user_name, distinct_char; cin>>user_name;

    for(int i=0; i<user_name.length(); i++){
        for(int j=i; j<user_name.length()-1; j++){
            if(user_name[i]user_name[j+1]){

            }
        }
    }

    return 0;
}
